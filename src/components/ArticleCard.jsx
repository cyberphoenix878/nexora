import { ArrowUpRight, Bookmark, Heart } from 'lucide-react'
import { Link } from 'react-router-dom'
export default function ArticleCard({ article, featured=false }) { const author=article.profiles||{}; return <article className={`article-card ${featured?'featured':''}`}><Link to={`/article/${article.slug}`} className="card-image" style={{backgroundImage:article.cover_image?`url(${article.cover_image})`:undefined}}><span className="image-fallback">{(article.categories?.name||'N').slice(0,1)}</span><span className="card-category">{article.categories?.name||'Nexora'}</span><span className="arrow"><ArrowUpRight size={17}/></span></Link><div className="card-body"><Link to={`/article/${article.slug}`}><h3>{article.title}</h3></Link><p>{article.excerpt}</p><div className="card-meta"><span className="author-mini">{(author.full_name||author.username||'N').slice(0,1).toUpperCase()}</span><span>{author.full_name||author.username||'Nexora'}</span><span>·</span><span>{article.reading_time||1} min read</span><span className="card-stats"><Heart size={13}/>{article.likes_count||0}<Bookmark size={13}/>{article.bookmarks_count||0}</span></div></div></article> }
